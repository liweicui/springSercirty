<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="com.ch.dao.UserDao">  
<select id="getUser" parameterType="java.util.HashMap" resultType="com.ch.model.User">  
    SELECT id as userId ,user_name as userName,pass_word as password FROM user WHERE user_name=#{username} AND pass_word=#{password}  
</select>    
</mapper>  